<% @user ||= current_user %>
<div class="stats">
  <span>
    <%= pluralize(@user.microposts.count, "micropost") %>
  </span>
  <span>
    <%= pluralize(@user.comments.count, "comment") %>
  </span>
</div>
